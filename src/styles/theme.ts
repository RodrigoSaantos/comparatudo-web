import { DefaultTheme } from 'styled-components'

const myTheme: DefaultTheme = {
  colors: {
    main: 'cyan',
    secondary: 'magenta',
  },

  background: 'var(--color-background)'
}

export { myTheme }